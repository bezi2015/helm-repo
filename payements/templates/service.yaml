apiVersion: v1
kind: Service
metadata:
  name: {{ include "payements.fullname" . }}
  labels:
    {{- include "payements.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payements.selectorLabels" . | nindent 4 }}
